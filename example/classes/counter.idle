(module
(class Counter
  (field value (mut i32))
  (method read ()
    (result i32)
    (classes:get Counter value $this))
  (method write ((value i32))
    (result)
    (classes:set Counter value $this $value)))

(export-new Counter)
(export-method Counter read)
(export-method Counter write)
)
